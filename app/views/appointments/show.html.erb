<p style="color: green"><%= notice %></p>

<%= render @appointment %>

<div>
  <%= link_to "Edit this appointment", edit_appointment_path(@appointment) %> |
  <%= link_to "Back to appointments", appointments_path %>

  <%= button_to "Destroy this appointment", @appointment, method: :delete %>
</div>
<div class="container">
  <h2 class="header mx-auto my-5"><%=@appointment.name%> Details</h2>
<hr>
<div class="card my-5 px-5" style="width: 18rem;">
<p style="color: green"><%= notice %></p><p style="color: green"><%= notice %></p>

<%= render @appointment %>

<button class="btn-warning">
  <%= link_to "Edit", edit_appointment_path(@appointment) %>
  </button>
  <button class="btn my-5">
    <%= link_to "Back", appointments_path %>
    </button>
</div>
</div>